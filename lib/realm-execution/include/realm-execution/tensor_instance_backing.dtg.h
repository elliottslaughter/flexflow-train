// THIS FILE WAS AUTO-GENERATED BY proj. DO NOT MODIFY IT!
// If you would like to modify this datatype, instead modify
// lib/realm-execution/include/realm-execution/tensor_instance_backing.dtg.toml
/* proj-data
{
  "generated_from": "f5415b39105185036ad312a6e5a2982e"
}
*/

#ifndef _FLEXFLOW_LIB_REALM_EXECUTION_INCLUDE_REALM_EXECUTION_TENSOR_INSTANCE_BACKING_DTG_H
#define _FLEXFLOW_LIB_REALM_EXECUTION_INCLUDE_REALM_EXECUTION_TENSOR_INSTANCE_BACKING_DTG_H

#include <tuple>
#include <iostream>
#include <map>
#include "task-spec/dynamic_graph/dynamic_value_attrs.dtg.h"
#include <ostream>
#include <fmt/format.h>
#include <nlohmann/json.hpp>
#include "realm-execution/realm.h"


namespace FlexFlow{

/**
 * \brief A simple container for mapping between DynamicValueAttrs and the corresponding Realm instances (along with the ready event for each instance).
 * 
 * \note The actual logic for doing instance allocation and destruction are in \ref perform_instance_allocation
 * and \ref destroy_instances, respectively.
 */struct TensorInstanceBacking{TensorInstanceBacking() = delete;
explicit TensorInstanceBacking(std::map<::FlexFlow::DynamicValueAttrs, std::pair<::FlexFlow::Realm::RegionInstance, ::FlexFlow::Realm::Event>> const &backing);

bool operator==(TensorInstanceBacking const &) const;bool operator!=(TensorInstanceBacking const &) const;
std::string debug_to_string() const;void debug_print() const;

/**
 * The need to track a pair of RegionInstance and Event, rather than just a RegionInstance, is due to the fact that
 * unlike in Legion, RegionInstance does not natively encode its own ready state. This gives you have a choice:
 * 
 * 1. Either block eagerly (to make sure all instances are ready and you never accidently use an instance before it becomes ready), or
 * 2. Carry the ready event around with the instance and block before you use it.
 * 
 * The way Realm works, an instance always has exactly one ready event (i.e., a
 * ready event is actually a unique key for the instance), so there's really only
 * one semantic meaning that makes sense here. Thus, it's idiomatic Realm coding style
 * to talk about any given asynchronous object handle as a pair of (Object handle,
 * ready Event for Object).
 */std::map<::FlexFlow::DynamicValueAttrs, std::pair<::FlexFlow::Realm::RegionInstance, ::FlexFlow::Realm::Event>> backing;
};}// namespace FlexFlow

namespace nlohmann{template <>
struct adl_serializer<::FlexFlow::TensorInstanceBacking>{static void to_json(json &, ::FlexFlow::TensorInstanceBacking const &);
};}// namespace nlohmann

namespace FlexFlow{std::string format_as(TensorInstanceBacking const &);
std::ostream &operator<<(std::ostream &, TensorInstanceBacking const &);}// namespace FlexFlow

#endif // _FLEXFLOW_LIB_REALM_EXECUTION_INCLUDE_REALM_EXECUTION_TENSOR_INSTANCE_BACKING_DTG_H
